# انتشار فایل‌های onFiBOX

عنوان و توضیحات عمومی Releases فارسی باشند. در توضیحات عمومی از عبارت «موبایل اندروید» استفاده کنید و عبارت «نسخهٔ جهانی» یا «Global» ننویسید. نام فایل‌های دانلود ثابت است:

- `onfibox.apk`: موبایل اندروید
- `onfiboxtv.apk`: Android TV
- `onFiBOX_PC_Cast.exe`: ویندوز

برای هر انتشار، اطلاعات `release-manifest.json` را با نسخه و هش واقعی همین سه فایل به‌روز کنید و همراه فایل‌های نصب در همان Release آپلود کنید. `release_tag` باید با برچسب انتشار برابر باشد. نسخه‌ها از روی فایل نصب یا تنظیمات بیلد قطعی استخراج شوند؛ از تاریخ انتشار یا متن توضیحات حدس زده نشوند.

بک‌اند هر ۱۵ دقیقه با GitHub API انتشار پایدار latest را می‌خواند و `release-manifest.json` را دریافت می‌کند. اگر یکی از سه فایل، نسخه یا هش معتبر نباشد، نسخهٔ قبلی حفظ می‌شود. پیش‌انتشارها وارد API عمومی به‌روزرسانی برنامه‌ها نمی‌شوند.

مقادیر `sha256` باید با digest فایل‌های asset در GitHub یکسان باشند. کد عددی `version_code` برای APKها ضروری است. توضیحات `release_notes.fa` مربوط به همان برنامه باشد. برای برگشت عمدی نسخه یا توقف همگام‌سازی یک برنامه، از حالت دستی در پنل مدیریت بک‌اند استفاده کنید.

لینک‌های دامنه به `releases/latest/download` اشاره می‌کنند. API داخلی بک‌اند لینک نسخه‌دار را می‌دهد تا هش و فایل هنگام انتشار نسخهٔ بعدی از هم جدا نشوند.
